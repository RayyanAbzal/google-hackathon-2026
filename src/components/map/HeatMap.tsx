'use client'

// Owner: Ray
// D3 choropleth heatmap of London by borough
// Colour scale: dark = no verified users, bright blue = high density
// Maalav embeds this in /map page
export function HeatMap() {
  return <div>HeatMap — TODO (Ray)</div>
}
