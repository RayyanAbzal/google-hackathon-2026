'use client'

// Owner: Maalav
// Register page — 3 steps: name+skill, PIN, doc upload
// Calls POST /api/auth/register
// On success: store civictrust_session in localStorage, redirect to /profile/[username]
export default function RegisterPage() {
  return <div>Register — TODO (Maalav)</div>
}
