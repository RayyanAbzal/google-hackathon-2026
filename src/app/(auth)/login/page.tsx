'use client'

// Owner: Maalav
// Login page — node_id OR @username + 4-digit PIN
// Calls POST /api/auth/login
// On success: store civictrust_session in localStorage, redirect to /profile/[username]
export default function LoginPage() {
  return <div>Login — TODO (Maalav)</div>
}
