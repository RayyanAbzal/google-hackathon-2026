'use client'

// Owner: Maalav
// Post for Help — requires auth (any score)
// Top: HelpPostForm | Below: HelpPostCards filtered by user's borough
// "Respond" button visible only if score >= 50
// Calls GET /api/help?borough=... + POST /api/help
export default function HelpPage() {
  return <div>Help — TODO (Maalav)</div>
}
